trigger:
  - main

pool:
  vmImage: 'ubuntu-latest'

steps:
  - script: |
      echo "Checking node version"
      node -v
    displayName: Check Node

  - script: |
      echo "About to fail"
      invalidcommand
      echo "Should not be seen"
    displayName: Fail Intentionally

  - script: |
      echo "This should NOT run"
    displayName: Should Be Skipped
